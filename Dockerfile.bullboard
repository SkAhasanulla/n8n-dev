# Build a tiny Bull Board server to inspect Redis queues
FROM node:18-alpine

WORKDIR /usr/src/app

# Install minimal dependencies
RUN apk add --no-cache git

# Use a small express + bull-board app (server.js)
COPY package.json package-lock.json* ./
RUN npm install --production

COPY server.js ./

EXPOSE 3002
CMD ["node", "server.js"]
